version: 2.1

# このプロジェクトで実行するジョブの定義
jobs:
  test:
    docker:
      - image: cimg/openjdk:17.0.7
    steps:
      - checkout
      - run:
          command: ./gradlew test --console=plain --info

# ジョブの実行順の指定
workflows:
  build_and_test:
    jobs:
      - test
